---
suites:
  event_dispatcher_suite:
    namespace: BigName\EventDispatcher
    psr4_prefix: BigName\EventDispatcher

formatter.name: pretty

extensions:
    - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
  format: clover
  output: coverage.xml